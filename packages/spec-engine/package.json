{
  "name": "@spec/engine",
  "version": "0.1.0",
  "description": "Core spec engine — parse, validate, simulate. Uses bun:sqlite ($0).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "bun run --check src/"
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}
