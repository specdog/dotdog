{
  "name": "@spec/mcp",
  "version": "0.1.0",
  "description": "MCP Server — AI agents query spec genomes via stdio ($0 hosting)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "lint": "bun run --check src/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@spec/engine": "workspace:*",
    "zod": "^3.23.0"
  }
}
