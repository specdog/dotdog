{
  "name": "@spec/cli",
  "version": "0.1.0",
  "description": "CLI — spec validate, simulate, init. Install globally or use via bun.",
  "main": "src/index.ts",
  "bin": {
    "spec": "src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "lint": "bun run --check src/"
  },
  "dependencies": {
    "@spec/engine": "workspace:*",
    "commander": "^12.0.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
