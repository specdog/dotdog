{
  "name": "dotdog-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Private workspace for the dotdog CLI package.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/dotdog build",
    "lint": "bun run --filter '*' lint",
    "test": "bun test",
    "pack:check": "node scripts/check-pack.mjs",
    "validate": "bun packages/spec-cli/src/index.ts validate"
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "dotdog": "workspace:*"
  }
}
